.venv/
__pycache__/
*.pyc
artifacts/
mlruns/
mlartifacts/
*.joblib
*.pt
.dvc/cache/
.dvc/tmp/
.env
reports/
*.egg-info/
*.egg-link
# Data files are DVC-tracked; the .dvc pointer files are committed to git.
# We ignore the raw data files but NOT the directory itself,
# so that git can see data/*.dvc pointer files and data/.gitignore.
data/raw_ebs/
data/features/
data/processed/

# Local tooling (editor / code-intelligence indexes — not part of the project)
.codegraph/
.cursor/
